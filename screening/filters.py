# filters.py
def apply_screen(df, rules):
    # Placeholder: returns all indicators passed and score
    details = {'rsi': {'passed': True, 'value': 50}}
    if df.empty:
    st.warning(f"No data for {symbol}")
    continue
    return True, details
